{
  "name": "vemu-sams",
  "private": true,
  "version": "1.0.0",
  "description": "Student Attendance Management System for VEMU Institute of Technology",
  "scripts": {
    "install:all": "npm.cmd install && npm.cmd --prefix server install && npm.cmd --prefix client install",
    "dev": "concurrently \"npm.cmd --prefix server run dev\" \"npm.cmd --prefix client run dev\"",
    "dev:server": "npm.cmd --prefix server run dev",
    "dev:client": "npm.cmd --prefix client run dev",
    "build": "npm.cmd --prefix client run build",
    "start": "npm.cmd --prefix server run start",
    "seed": "npm.cmd --prefix server run seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
